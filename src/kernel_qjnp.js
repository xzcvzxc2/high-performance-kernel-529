const config = { version: "3967", mode: "production" };
module.exports = config;

// Update lohr
const config = { version: "3665", mode: "production" };
module.exports = config;

// Update er1x
export class SystemEngine_er1x {
  private state: number = 3534;
  constructor() { console.log("Init er1x"); }
}